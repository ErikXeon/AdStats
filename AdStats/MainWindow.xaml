<Window x:Class="AdStats.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ПДАРК — Анализ рекламных кампаний"
        Height="892"
        Width="1420"
        MinHeight="760"
        MinWidth="1240"
        WindowStartupLocation="CenterScreen"
        Background="#0B1020"
        Foreground="#E5E7EB">
    <Window.Resources>
        <SolidColorBrush x:Key="CardBrush" Color="#121A33"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#5EEAD4"/>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1C2547"/>
            <Setter Property="Foreground" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#2F3C67"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,6,0,8"/>
        </Style>
        <Style x:Key="InputComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#1C2547"/>
            <Setter Property="Foreground" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#2F3C67"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="0,6,0,8"/>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="0,6,8,6"/>
            <Setter Property="Padding" Value="16,9"/>
            <Setter Property="Background" Value="#14B8A6"/>
            <Setter Property="Foreground" Value="#042F2E"/>
            <Setter Property="BorderBrush" Value="#14B8A6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#334155"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
        </Style>
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#DC2626"/>
            <Setter Property="BorderBrush" Value="#DC2626"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="KpiValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
        </Style>
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#121A33"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="RowBackground" Value="#121A33"/>
            <Setter Property="AlternatingRowBackground" Value="#1A2346"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="BorderBrush" Value="#243156"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="ПДАРК" FontSize="34" FontWeight="Bold" Foreground="#E2E8F0"/>
                <TextBlock Text="Панель анализа эффективности рекламных кампаний" Foreground="#93C5FD" FontSize="14"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock x:Name="TextLastSync" Foreground="#CBD5E1" HorizontalAlignment="Right"/>
                <TextBlock x:Name="TextDbPath" Foreground="#64748B" FontSize="12" HorizontalAlignment="Right"/>
            </StackPanel>
        </DockPanel>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource CardBorderStyle}" Grid.Column="0" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Общий бюджет" Foreground="#94A3B8"/>
                    <TextBlock x:Name="KpiTotalBudget" Style="{StaticResource KpiValueStyle}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Grid.Column="1" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Средний ROI" Foreground="#94A3B8"/>
                    <TextBlock x:Name="KpiAvgRoi" Style="{StaticResource KpiValueStyle}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Grid.Column="2" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Средний CTR" Foreground="#94A3B8"/>
                    <TextBlock x:Name="KpiAvgCtr" Style="{StaticResource KpiValueStyle}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Grid.Column="3" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Всего лидов" Foreground="#94A3B8"/>
                    <TextBlock x:Name="KpiTotalConversions" Style="{StaticResource KpiValueStyle}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Grid.Column="4">
                <StackPanel>
                    <TextBlock Text="Активных кампаний" Foreground="#94A3B8"/>
                    <TextBlock x:Name="KpiActiveCampaigns" Style="{StaticResource KpiValueStyle}"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Style="{StaticResource CardBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="Карточка кампании" FontSize="18" FontWeight="SemiBold"/>

                            <TextBlock Text="Название" Margin="0,14,0,0"/>
                            <TextBox x:Name="InputName" Style="{StaticResource InputTextBoxStyle}"/>

                            <TextBlock Text="Канал"/>
                            <ComboBox x:Name="InputChannel" Style="{StaticResource InputComboBoxStyle}">
                                <ComboBoxItem Content="Google Ads"/>
                                <ComboBoxItem Content="VK Ads"/>
                                <ComboBoxItem Content="Yandex Direct"/>
                                <ComboBoxItem Content="Telegram Ads"/>
                                <ComboBoxItem Content="Meta Ads"/>
                            </ComboBox>

                            <TextBlock Text="Статус"/>
                            <ComboBox x:Name="InputStatus" Style="{StaticResource InputComboBoxStyle}">
                                <ComboBoxItem Content="Draft"/>
                                <ComboBoxItem Content="Active"/>
                                <ComboBoxItem Content="Paused"/>
                                <ComboBoxItem Content="Completed"/>
                            </ComboBox>

                            <TextBlock Text="Бюджет (₽)"/>
                            <TextBox x:Name="InputBudget" Style="{StaticResource InputTextBoxStyle}"/>

                            <TextBlock Text="Показы"/>
                            <TextBox x:Name="InputImpressions" Style="{StaticResource InputTextBoxStyle}"/>

                            <TextBlock Text="Клики"/>
                            <TextBox x:Name="InputClicks" Style="{StaticResource InputTextBoxStyle}"/>

                            <TextBlock Text="Конверсии"/>
                            <TextBox x:Name="InputConversions" Style="{StaticResource InputTextBoxStyle}"/>

                            <TextBlock Text="Доход (₽)"/>
                            <TextBox x:Name="InputRevenue" Style="{StaticResource InputTextBoxStyle}"/>

                            <WrapPanel Margin="0,10,0,0">
                                <Button Content="Добавить" Style="{StaticResource PrimaryButtonStyle}" Click="AddCampaign_Click"/>
                                <Button Content="Обновить" Style="{StaticResource SecondaryButtonStyle}" Click="UpdateCampaign_Click"/>
                                <Button Content="Удалить" Style="{StaticResource DangerButtonStyle}" Click="DeleteCampaign_Click"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="Прогнозатор" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock Text="Рост бюджета (%)" Margin="0,12,0,0"/>
                            <TextBox x:Name="InputBudgetGrowth" Style="{StaticResource InputTextBoxStyle}" Text="15"/>
                            <Button Content="Рассчитать прогноз" Style="{StaticResource SecondaryButtonStyle}" Click="Forecast_Click"/>
                            <TextBlock x:Name="TextForecast" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="#A7F3D0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Style="{StaticResource CardBorderStyle}" Grid.Row="0">
                    <DockPanel>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                            <TextBlock Text="Поиск:" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBox x:Name="InputSearch" Width="220" Style="{StaticResource InputTextBoxStyle}" Margin="0" TextChanged="FilterChanged"/>
                            <TextBlock Text="Канал:" Margin="16,0,8,0" VerticalAlignment="Center"/>
                            <ComboBox x:Name="FilterChannel" Width="150" Style="{StaticResource InputComboBoxStyle}" Margin="0" SelectionChanged="FilterChanged">
                                <ComboBoxItem Content="Все" IsSelected="True"/>
                                <ComboBoxItem Content="Google Ads"/>
                                <ComboBoxItem Content="VK Ads"/>
                                <ComboBoxItem Content="Yandex Direct"/>
                                <ComboBoxItem Content="Telegram Ads"/>
                                <ComboBoxItem Content="Meta Ads"/>
                            </ComboBox>
                            <TextBlock Text="Статус:" Margin="16,0,8,0" VerticalAlignment="Center"/>
                            <ComboBox x:Name="FilterStatus" Width="150" Style="{StaticResource InputComboBoxStyle}" Margin="0" SelectionChanged="FilterChanged">
                                <ComboBoxItem Content="Все" IsSelected="True"/>
                                <ComboBoxItem Content="Draft"/>
                                <ComboBoxItem Content="Active"/>
                                <ComboBoxItem Content="Paused"/>
                                <ComboBoxItem Content="Completed"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                            <Button Content="Сохранить" Style="{StaticResource SecondaryButtonStyle}" Click="SaveToFile_Click"/>
                            <Button Content="Сбросить форму" Style="{StaticResource SecondaryButtonStyle}" Click="ClearForm_Click"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}" Grid.Row="1" Margin="0,0,0,0">
                    <DataGrid x:Name="CampaignGrid"
                              Style="{StaticResource DataGridStyle}"
                              SelectionChanged="CampaignGrid_SelectionChanged"
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="2*"/>
                            <DataGridTextColumn Header="Канал" Binding="{Binding Channel}" Width="*"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="*"/>
                            <DataGridTextColumn Header="Бюджет" Binding="{Binding Budget, StringFormat={}{0:N0} ₽}" Width="*"/>
                            <DataGridTextColumn Header="CTR" Binding="{Binding CtrPercent, StringFormat={}{0:F2}%}" Width="*"/>
                            <DataGridTextColumn Header="CPC" Binding="{Binding Cpc, StringFormat={}{0:F2} ₽}" Width="*"/>
                            <DataGridTextColumn Header="CPA" Binding="{Binding Cpa, StringFormat={}{0:F2} ₽}" Width="*"/>
                            <DataGridTextColumn Header="ROI" Binding="{Binding RoiPercent, StringFormat={}{0:F2}%}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Grid>

        <StatusBar Grid.Row="3" Background="#0F172A" Foreground="#93C5FD">
            <StatusBarItem>
                <TextBlock x:Name="TextStatus" Text="Готово"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
