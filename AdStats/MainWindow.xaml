<Window x:Class="AdStats.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ПДАРК — Анализ рекламных кампаний"
        Height="892"
        Width="1420"
        MinHeight="760"
        MinWidth="1240"
        WindowStartupLocation="CenterScreen"
        Background="#0B1020"
        Foreground="#E5E7EB">
    <Window.Resources>
        <SolidColorBrush x:Key="CardBrush" Color="#121A33"/>
        <SolidColorBrush x:Key="CardSoftBrush" Color="#1A2346"/>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
        </Style>
        <Style x:Key="InputLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="Margin" Value="0,10,0,4"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1C2547"/>
            <Setter Property="Foreground" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#2F3C67"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
        </Style>
        <Style x:Key="InputLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="Margin" Value="0,10,0,4"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="ComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1C2547"/>
            <Setter Property="Foreground" Value="#F3F4F6"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>
        <Style x:Key="InputComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#1C2547"/>
            <Setter Property="Foreground" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="#2F3C67"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#334155"/>
        </Style>
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#B91C1C"/>
        </Style>
        <Style x:Key="KpiValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#5EEAD4"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="#121A33"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="RowBackground" Value="#121A33"/>
            <Setter Property="AlternatingRowBackground" Value="#1A2346"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="BorderBrush" Value="#243156"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,14" Padding="16,14">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="ПДАРК" FontSize="30" FontWeight="Bold" Foreground="#E2E8F0"/>
                    <TextBlock Text="Простая панель учёта и анализа рекламных кампаний" Foreground="#93C5FD" FontSize="14"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock x:Name="TextLastSync" Foreground="#CBD5E1" HorizontalAlignment="Right"/>
                    <TextBlock x:Name="TextDbPath" Foreground="#64748B" FontSize="12" HorizontalAlignment="Right"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <UniformGrid Grid.Row="1" Columns="5" Margin="0,0,0,14">
            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Общий бюджет" Foreground="#94A3B8"/>
                    <TextBlock x:Name="KpiTotalBudget" Style="{StaticResource KpiValueStyle}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,10,0">                <StackPanel>
                    <TextBlock Text="Окупаемость рекламы" Foreground="#94A3B8"/>
                    <TextBlock x:Name="KpiAvgRoi" Style="{StaticResource KpiValueStyle}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,10,0">                <StackPanel>
                    <TextBlock Text="Кликов на 100 показов" Foreground="#94A3B8"/>
                    <TextBlock x:Name="KpiAvgCtr" Style="{StaticResource KpiValueStyle}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Всего заявок" Foreground="#94A3B8"/>
                    <TextBlock x:Name="KpiTotalConversions" Style="{StaticResource KpiValueStyle}"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Margin="0">
                <StackPanel>
                    <TextBlock Text="Активные рекламы" Foreground="#94A3B8"/>
                    <TextBlock x:Name="KpiActiveCampaigns" Style="{StaticResource KpiValueStyle}"/>
                </StackPanel>
            </Border>
        </UniformGrid>
        
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="390"/>
                <ColumnDefinition Width="14"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border Style="{StaticResource CardBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="Шаг 1. Заполните данные кампании" FontSize="18" FontWeight="SemiBold"/>
                            
                            <TextBlock Text="Название" Margin="0,14,0,0"/>
                            <TextBlock Text="Название кампании" Style="{StaticResource InputLabelStyle}"/>

                            <TextBlock Text="Канал продвижения" Style="{StaticResource InputLabelStyle}"/>
                            <ComboBox x:Name="InputChannel" Style="{StaticResource InputComboBoxStyle}">
                                <ComboBoxItem Content="Google Ads"/>
                                <ComboBoxItem Content="VK Ads"/>
                                <ComboBoxItem Content="Yandex Direct"/>
                                <ComboBoxItem Content="Telegram Ads"/>
                                <ComboBoxItem Content="Meta Ads"/>
                            </ComboBox>

                            <TextBlock Text="Статус" Style="{StaticResource InputLabelStyle}"/>
                            <ComboBox x:Name="InputStatus" Style="{StaticResource InputComboBoxStyle}">
                                <ComboBoxItem Content="Черновик"/>
                                <ComboBoxItem Content="Запущена"/>
                                <ComboBoxItem Content="На паузе"/>
                                <ComboBoxItem Content="Завершена"/>
                            </ComboBox>

                            <Grid Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Бюджет (₽)" Style="{StaticResource InputLabelStyle}" Margin="0,6,0,4"/>
                                    <TextBox x:Name="InputBudget" Style="{StaticResource InputTextBoxStyle}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Доход (₽)" Style="{StaticResource InputLabelStyle}" Margin="0,6,0,4"/>
                                    <TextBox x:Name="InputRevenue" Style="{StaticResource InputTextBoxStyle}"/>
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Показы" Style="{StaticResource InputLabelStyle}" Margin="0,6,0,4"/>
                                    <TextBox x:Name="InputImpressions" Style="{StaticResource InputTextBoxStyle}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Клики" Style="{StaticResource InputLabelStyle}" Margin="0,6,0,4"/>
                                    <TextBox x:Name="InputClicks" Style="{StaticResource InputTextBoxStyle}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="4">
                                    <TextBlock Text="Конверсии" Style="{StaticResource InputLabelStyle}" Margin="0,6,0,4"/>
                                    <TextBox x:Name="InputConversions" Style="{StaticResource InputTextBoxStyle}"/>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Шаг 2. Выберите действие" Foreground="#94A3B8" Margin="0,12,0,8" FontSize="12"/>
                            <WrapPanel>
                                <Button Content="Добавить" Style="{StaticResource PrimaryButtonStyle}" Click="AddCampaign_Click"/>
                                <Button Content="Обновить" Style="{StaticResource SecondaryButtonStyle}" Click="UpdateCampaign_Click"/>
                                <Button Content="Удалить" Style="{StaticResource DangerButtonStyle}" Click="DeleteCampaign_Click"/>
                            </WrapPanel>
                            <WrapPanel>
                                <Button Content="Сохранить" Style="{StaticResource SecondaryButtonStyle}" Click="SaveToFile_Click"/>
                                <Button Content="Сбросить форму" Style="{StaticResource SecondaryButtonStyle}" Click="ClearForm_Click"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="Сценарий изменения бюджета" Foreground="#94A3B8" Margin="0,8,0,4"/>
                            <DockPanel Margin="0,0,0,8">
                                <TextBox x:Name="InputBudgetGrowth" Style="{StaticResource InputTextBoxStyle}" Text="15" Width="100"/>
                                <TextBlock Text="%" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="#CBD5E1"/>
                            </DockPanel>
                            <Button Content="Рассчитать прогноз" Style="{StaticResource SecondaryButtonStyle}" Click="Forecast_Click" HorizontalAlignment="Left"/>
                            <Border Background="{StaticResource CardSoftBrush}" CornerRadius="10" Padding="10" Margin="0,10,0,0">
                                <TextBlock x:Name="TextForecast" TextWrapping="Wrap" Foreground="#A7F3D0" Text="Здесь появится прогноз по бюджету, выручке и ROI."/>
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Style="{StaticResource CardBorderStyle}" Grid.Row="0">
                    <StackPanel>
                        <TextBlock Text="Список кампаний" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock Text="Фильтры и поиск" Foreground="#94A3B8" Margin="0,8,0,8"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="InputSearch" Grid.Column="0" Style="{StaticResource InputTextBoxStyle}" TextChanged="FilterChanged"/>
                            <ComboBox x:Name="FilterChannel" Grid.Column="2" Style="{StaticResource InputComboBoxStyle}" SelectionChanged="FilterChanged">
                                
                                <ComboBoxItem Content="Все" IsSelected="True"/>
                                <ComboBoxItem Content="Google Ads"/>
                                <ComboBoxItem Content="VK Ads"/>
                                <ComboBoxItem Content="Yandex Direct"/>
                                <ComboBoxItem Content="Telegram Ads"/>
                                <ComboBoxItem Content="Meta Ads"/>
                            </ComboBox>
                            <ComboBox x:Name="FilterStatus" Grid.Column="4" Style="{StaticResource InputComboBoxStyle}" SelectionChanged="FilterChanged">
                            <ComboBox x:Name="FilterStatus" Width="150" Style="{StaticResource InputComboBoxStyle}" Margin="0" SelectionChanged="FilterChanged">
                                <ComboBoxItem Content="Все" IsSelected="True"/>
                                <ComboBoxItem Content="Черновик"/>
                                <ComboBoxItem Content="Запущена"/>
                                <ComboBoxItem Content="На паузе"/>
                                <ComboBoxItem Content="Завершена"/>
                            </ComboBox>
                        </Grid>
                        <WrapPanel Margin="0,10,0,0">
                            <Button Content="Сохранить" Style="{StaticResource SecondaryButtonStyle}" Click="SaveToFile_Click"/>
                            <Button Content="Сбросить форму" Style="{StaticResource SecondaryButtonStyle}" Click="ClearForm_Click"/>
                        </WrapPanel>
                    </StackPanel>

                    <Border Style="{StaticResource CardBorderStyle}" Grid.Row="1" Margin="0">
                        <DataGrid x:Name="CampaignGrid"
                              Style="{StaticResource DataGridStyle}"
                              SelectionChanged="CampaignGrid_SelectionChanged"
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="2*"/>
                            <DataGridTextColumn Header="Канал" Binding="{Binding Channel}" Width="*"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="*"/>
                            <DataGridTextColumn Header="Бюджет" Binding="{Binding Budget, StringFormat={}{0:N0} ₽}" Width="*"/>
                            <DataGridTextColumn Header="Кликов на 100 показов" Binding="{Binding CtrPercent, StringFormat={}{0:F2}%}" Width="*"/>
                            <DataGridTextColumn Header="Цена 1 клика" Binding="{Binding Cpc, StringFormat={}{0:F2} ₽}" Width="*"/>
                            <DataGridTextColumn Header="Цена 1 заявки" Binding="{Binding Cpa, StringFormat={}{0:F2} ₽}" Width="*"/>
                            <DataGridTextColumn Header="Окупаемость" Binding="{Binding RoiPercent, StringFormat={}{0:F2}%}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Grid>

        <StatusBar Grid.Row="3" Background="#0F172A" Foreground="#93C5FD">
            <StatusBarItem>
                <TextBlock x:Name="TextStatus" Text="Готово"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
